machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t philipz/rpi-mysql .

#test:
#  override:
#    - docker run -ti --rm philipz/rpi-mysql qemu-arm-static /bin/sh -c 'mysql --version'

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push philipz/rpi-mysql
